import React from 'react';
import { Card, CardHeader, CardText, CardTitle, CardMedia } from 'material-ui/Card';

export default () => (
  <div>
    <Card>
      <CardTitle title="Builds" />
      <CardText>Lorem ipsum sic dolor amet...</CardText>
    </Card>
  </div>
);